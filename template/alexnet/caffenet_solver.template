net: "${caffe_train_model}"
test_iter: 67
test_interval: 100
base_lr: ${learning_rate}
lr_policy: "step"
gamma: 0.1
stepsize: ${stepsize}
display: 20
max_iter: ${max_iter}
momentum: 0.9
weight_decay: 0.0005
snapshot: 1000
snapshot_prefix: "${snapshot_prefix}"
solver_mode: ${solver_mode}